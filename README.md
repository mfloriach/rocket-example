# Example of Rocket server using Diesel

Simple server using Rust with Rocket framework and diesel for ORM.

## Installation

```
$ docker-compose up -d
$ diesel migrate  run
$ cargo watch -w src -x run
```
